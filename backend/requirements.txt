# Giữ mỗi thư viện trong cùng major version để nhận bản vá tương thích nhưng
# không tự động kéo API phá vỡ hành vi khi cài trên một máy mới.
# Framework HTTP API và dependency injection.
fastapi>=0.100.0,<1.0.0
# ASGI server chạy FastAPI.
uvicorn>=0.23.0,<1.0.0
# ORM và transaction bất đồng bộ.
sqlalchemy>=2.0.0,<3.0.0
# Driver PostgreSQL async dùng bởi SQLAlchemy.
asyncpg>=0.28.0,<1.0.0
# Kiểm tra/chuyển đổi schema request-response.
pydantic>=2.0.0,<3.0.0
# Đọc Settings từ biến môi trường và tệp .env.
pydantic-settings>=2.0.0,<3.0.0
# MQTT client kết nối broker và nhận telemetry.
paho-mqtt>=1.6.1,<3.0.0
# Hỗ trợ giao thức WebSocket trong stack ASGI.
websockets>=11.0.3,<18.0.0
# Quản lý phiên bản schema database.
alembic>=1.11.0,<2.0.0
# Kiểu/câu lệnh không gian PostGIS cho SQLAlchemy.
GeoAlchemy2>=0.14.1,<1.0.0
# Phát và xác minh JSON Web Token.
PyJWT>=2.10.0,<3.0.0
# Băm/kiểm tra mật khẩu bằng Argon2.
pwdlib[argon2]>=0.3.0,<1.0.0
# Cơ sở dữ liệu múi giờ cho máy thiếu timezone hệ thống đầy đủ.
tzdata>=2024.1
